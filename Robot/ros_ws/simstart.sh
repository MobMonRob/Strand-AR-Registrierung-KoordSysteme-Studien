## for some reason the new terminal doesn't work with any program call

# create a new terminal in same directory in sim environment
#gnome-terminal -e ./intera.sh sim
# launch simulation with gazebo
roslaunch sawyer_gazebo sawyer_world.launch
